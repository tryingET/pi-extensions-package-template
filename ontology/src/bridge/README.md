---
summary: "ROCS bridge ontology notes for pi-extensions-template."
read_when:
  - "Working with pi-extensions-template ROCS bridge ontology files."
type: "reference"
---

# Bridge

The bridge layer links ontology concepts to repo artifacts (code symbols, APIs, schemas).

- `mapping.yaml` maps stable concept IDs → repo-local symbols/paths.

