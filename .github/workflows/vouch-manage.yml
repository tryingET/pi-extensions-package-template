name: vouch-manage

on:
  issue_comment:
    types:
      - created

concurrency:
  group: vouch-manage-${{ github.repository }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  manage:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: read
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Apply vouch / denounce / unvouch from maintainer comments
        uses: mitchellh/vouch/action/manage-by-issue@3dbc69c691b8832a0dbb3913f47563b7c16c99f7
        with:
          repo: ${{ github.repository }}
          issue-id: ${{ github.event.issue.number }}
          comment-id: ${{ github.event.comment.id }}
          vouched-file: .github/VOUCHED.td
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
